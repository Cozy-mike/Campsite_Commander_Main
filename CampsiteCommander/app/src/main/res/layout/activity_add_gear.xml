<?xml version="1.0" encoding="utf-8"?>
<!-- Add Gear Screen: Form to add a new item to the gear arrays -->
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/forest_dark">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Header -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="➕ Add Gear to Pack"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/golden_yellow"
            android:layout_marginBottom="24dp"/>

        <!-- Item Name -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Item Name *"
            android:textColor="@color/light_green"
            android:textSize="14sp"
            android:layout_marginBottom="4dp"/>
        <EditText
            android:id="@+id/etItemName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="e.g. Tent, Flashlight..."
            android:inputType="text"
            android:textColor="@color/white"
            android:textColorHint="@color/hint_color"
            android:backgroundTint="@color/light_green"
            android:layout_marginBottom="16dp"/>

        <!-- Category Spinner -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Category *"
            android:textColor="@color/light_green"
            android:textSize="14sp"
            android:layout_marginBottom="4dp"/>
        <Spinner
            android:id="@+id/spinnerCategory"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/light_green"
            android:layout_marginBottom="16dp"/>

        <!-- Quantity -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Quantity *"
            android:textColor="@color/light_green"
            android:textSize="14sp"
            android:layout_marginBottom="4dp"/>
        <EditText
            android:id="@+id/etQuantity"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="e.g. 1, 2, 3..."
            android:inputType="number"
            android:textColor="@color/white"
            android:textColorHint="@color/hint_color"
            android:backgroundTint="@color/light_green"
            android:layout_marginBottom="16dp"/>

        <!-- Comments / Notes -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Notes / Comments"
            android:textColor="@color/light_green"
            android:textSize="14sp"
            android:layout_marginBottom="4dp"/>
        <EditText
            android:id="@+id/etComments"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="e.g. Check batteries (AA)..."
            android:inputType="textMultiLine"
            android:lines="3"
            android:gravity="top"
            android:textColor="@color/white"
            android:textColorHint="@color/hint_color"
            android:backgroundTint="@color/light_green"
            android:layout_marginBottom="32dp"/>

        <!-- Save Button -->
        <Button
            android:id="@+id/btnSaveGear"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="💾  Save to Pack"
            android:textSize="16sp"
            android:backgroundTint="@color/button_green"
            android:textColor="@color/white"
            android:padding="16dp"
            android:layout_marginBottom="12dp"/>

        <!-- Back Button -->
        <Button
            android:id="@+id/btnBackFromAdd"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="← Back to Base"
            android:textSize="16sp"
            android:backgroundTint="@color/button_brown"
            android:textColor="@color/white"
            android:padding="16dp"/>

    </LinearLayout>
</ScrollView>
